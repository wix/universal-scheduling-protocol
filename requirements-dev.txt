# Development-only dependencies for tools/usp_check.py.
# Kept separate from requirements.txt so the mkdocs site build does not install
# a validator it never runs.
jsonschema>=4.21
referencing>=0.33
# Optional: enables real signature verification of the proof test vectors.
# Without it the vectors still round-trip their JCS digests, but the signatures
# are taken on trust - which is the one thing review cannot catch.
cryptography>=42.0
