PROCEED

The landed webhook-signing change is independent of registry category filter semantics. It modifies REST and MCP conformance checklist text far from this feature's Sections 3.3 and 6, plus transport site pages outside this plan.

The merge-tree preview reports both branches changed `specification.md`, but their hunks are disjoint and merge cleanly. `CHANGE_LOG.md` is append-only on both branches and requires preserving both newest entries during rebase.

Re-evaluated `PLAN.md` end to end:

- Goal and non-goals remain unchanged.
- All must-preserve constraints remain valid.
- U1 through U3 remain valid and complete.
- U4 remains necessary for the required change log entry, final policy cleanup, and fresh verification.
- No file action or verification command needs revision.
