{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://usp.dev/schemas/services/scheduling.json",
  "title": "USP Scheduling",
  "description": "Booking lifecycle schema for the Universal Scheduling Protocol. Defines Booking, Buyer, BookingPayment, PaymentContext, Action, Message, and Cancellation objects.",
  "$defs": {
    "Booking": {
      "type": "object",
      "title": "Booking",
      "description": "A scheduled service instance for a specific buyer at a specific time.",
      "required": [
        "id",
        "service_id",
        "service_name",
        "slot",
        "buyer",
        "party_size",
        "status",
        "confirmation_mode",
        "created_at",
        "updated_at"
      ],
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique booking identifier, generated by the business."
        },
        "service_id": {
          "type": "string",
          "description": "The booked service."
        },
        "service_name": {
          "type": "string",
          "description": "Service display name, captured at booking time. Snapshot that does not change if the service name is later updated."
        },
        "slot": {
          "$ref": "#/$defs/SlotReference",
          "description": "The booked time slot."
        },
        "buyer": {
          "$ref": "#/$defs/Buyer",
          "description": "The person making and paying for the booking. When no recipient is provided, the buyer is also the person receiving the service."
        },
        "recipient": {
          "$ref": "#/$defs/Buyer",
          "description": "The person receiving the service, when different from the buyer (e.g., a parent booking for a child, an assistant booking for their employer, or a gift booking). When absent, the buyer is the recipient."
        },
        "party_size": {
          "type": "integer",
          "description": "Total number of attendees. For appointment types typically 1. For group/reservation reflects spots booked."
        },
        "resources": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "id": {
                "type": "string"
              },
              "type": {
                "type": "string"
              },
              "name": {
                "type": "string"
              }
            }
          },
          "description": "The specific resources assigned to this booking."
        },
        "location": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string"
            },
            "name": {
              "type": "string"
            }
          },
          "description": "The specific location for this booking."
        },
        "status": {
          "type": "string",
          "enum": [
            "pending",
            "requires_action",
            "confirmed",
            "in_progress",
            "completed",
            "no_show",
            "canceled"
          ],
          "description": "Current booking status."
        },
        "confirmation_mode": {
          "type": "string",
          "enum": [
            "auto",
            "manual"
          ],
          "description": "Reflects the service's confirmation policy at booking time."
        },
        "payment": {
          "$ref": "#/$defs/BookingPayment",
          "description": "Payment state. MUST be present when requires_payment is true and payment_timing is at_booking or deposit_required."
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Action"
          },
          "description": "Ordered array of pending tasks the buyer must complete. MUST be present and non-empty when status is requires_action; MUST be absent or empty otherwise. The booking has status requires_action if and only if this array contains at least one action with status pending."
        },
        "notes": {
          "type": "string",
          "description": "Buyer-provided special requests or notes."
        },
        "cancellation": {
          "$ref": "#/$defs/Cancellation",
          "description": "Present when the booking has been canceled."
        },
        "created_at": {
          "type": "string",
          "format": "date-time",
          "description": "RFC 3339 timestamp of when the booking was created."
        },
        "updated_at": {
          "type": "string",
          "format": "date-time",
          "description": "RFC 3339 timestamp of the last status change or modification."
        },
        "expires_at": {
          "type": "string",
          "format": "date-time",
          "description": "RFC 3339 expiration time. Present for pending and requires_action bookings."
        }
      }
    },
    "SlotReference": {
      "type": "object",
      "title": "SlotReference",
      "description": "Reference to a booked time slot.",
      "required": [
        "id",
        "start",
        "end",
        "duration"
      ],
      "properties": {
        "id": {
          "type": "string"
        },
        "start": {
          "type": "string",
          "format": "date-time"
        },
        "end": {
          "type": "string",
          "format": "date-time"
        },
        "duration": {
          "type": "string",
          "description": "ISO 8601 duration"
        }
      }
    },
    "Buyer": {
      "type": "object",
      "title": "Buyer",
      "description": "The person making and paying for the booking. When no separate recipient is specified, the buyer is also the person receiving the service.",
      "required": [
        "first_name",
        "last_name",
        "email"
      ],
      "properties": {
        "first_name": {
          "type": "string"
        },
        "last_name": {
          "type": "string"
        },
        "email": {
          "type": "string",
          "format": "email"
        },
        "phone_number": {
          "type": "string"
        }
      }
    },
    "BookingPayment": {
      "type": "object",
      "title": "BookingPayment",
      "description": "Payment state within a booking. Tracks the lifecycle of payment.",
      "required": [
        "status",
        "timing"
      ],
      "properties": {
        "status": {
          "type": "string",
          "enum": [
            "not_required",
            "pending",
            "deposit_paid",
            "paid",
            "refunded",
            "partially_refunded"
          ],
          "description": "Payment status."
        },
        "timing": {
          "type": "string",
          "enum": [
            "at_booking",
            "at_service",
            "deposit_required"
          ],
          "description": "Mirrors the service's payment_timing value."
        },
        "amount": {
          "type": "integer",
          "description": "Total service amount in minor currency units. REQUIRED when timing is at_booking or deposit_required."
        },
        "currency": {
          "type": "string",
          "description": "ISO 4217 currency code. REQUIRED when amount is present."
        },
        "amount_due": {
          "type": "integer",
          "description": "Amount due now in minor currency units. REQUIRED when timing is at_booking or deposit_required."
        },
        "deposit_amount": {
          "type": "integer",
          "description": "The deposit amount in minor currency units. Present when timing is deposit_required."
        },
        "transaction_id": {
          "type": "string",
          "description": "Transaction identifier from the payment provider. Set after payment is confirmed."
        },
        "order_reference": {
          "type": "string",
          "description": "External order identifier from the checkout system. Used for refund reconciliation."
        }
      }
    },
    "PaymentContext": {
      "type": "object",
      "title": "PaymentContext",
      "description": "Universal payment handoff object nested inside a payment action. Contains everything a checkout system needs to process payment. The payment expiry deadline is carried on the action's expires_at field.",
      "required": [
        "amount_due",
        "currency",
        "description",
        "line_items",
        "metadata"
      ],
      "properties": {
        "amount_due": {
          "type": "integer",
          "description": "Amount to collect in minor currency units."
        },
        "currency": {
          "type": "string",
          "description": "ISO 4217 currency code."
        },
        "description": {
          "type": "string",
          "description": "Human-readable description of the payment."
        },
        "line_items": {
          "type": "array",
          "items": {
            "type": "object",
            "required": [
              "label",
              "amount",
              "quantity",
              "item_id"
            ],
            "properties": {
              "label": {
                "type": "string"
              },
              "amount": {
                "type": "integer"
              },
              "quantity": {
                "type": "integer"
              },
              "item_id": {
                "type": "string",
                "description": "Service ID for checkout system line item mapping."
              }
            }
          },
          "description": "Itemized breakdown for display and checkout integration."
        },
        "metadata": {
          "type": "object",
          "required": [
            "booking_id",
            "service_id",
            "service_type"
          ],
          "properties": {
            "booking_id": {
              "type": "string"
            },
            "service_id": {
              "type": "string"
            },
            "service_type": {
              "type": "string"
            },
            "slot_start": {
              "type": "string",
              "format": "date-time"
            }
          },
          "description": "Machine-readable context for the checkout system to pass back during confirmation."
        }
      }
    },
    "Action": {
      "type": "object",
      "title": "Action",
      "description": "A pending task the buyer must complete before a booking can be confirmed. Each action has a type, status, continue URL, and expiry.",
      "required": [
        "type",
        "status",
        "continue_url",
        "expires_at"
      ],
      "properties": {
        "type": {
          "type": "string",
          "description": "Action type identifier (e.g., payment, waiver)."
        },
        "status": {
          "type": "string",
          "enum": [
            "pending",
            "completed",
            "expired"
          ],
          "description": "Action status."
        },
        "continue_url": {
          "type": "string",
          "format": "uri",
          "description": "Business-hosted URL for completing this specific action."
        },
        "expires_at": {
          "type": "string",
          "format": "date-time",
          "description": "RFC 3339 deadline for this action. Can be equal to or earlier than the booking expiry."
        },
        "message": {
          "$ref": "#/$defs/Message",
          "description": "Human-readable context about this action."
        },
        "payment_context": {
          "$ref": "#/$defs/PaymentContext",
          "description": "Payment handoff object. MUST be present when type is payment (Standalone Mode only)."
        }
      }
    },
    "Message": {
      "type": "object",
      "title": "Message",
      "description": "Message providing context about the booking state.",
      "required": [
        "type",
        "code",
        "content"
      ],
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "error",
            "warning",
            "info"
          ],
          "description": "Message type."
        },
        "code": {
          "type": "string",
          "description": "Machine-readable code."
        },
        "content": {
          "type": "string",
          "description": "Human-readable text."
        },
        "severity": {
          "type": "string",
          "enum": [
            "requires_buyer_input",
            "recoverable",
            "requires_buyer_review"
          ],
          "description": "Severity level."
        },
        "path": {
          "type": "string",
          "description": "Optional JSON Pointer to the relevant field."
        }
      }
    },
    "Cancellation": {
      "type": "object",
      "title": "Cancellation",
      "description": "Cancellation details when a booking has been canceled.",
      "properties": {
        "reason": {
          "type": "string"
        },
        "canceled_by": {
          "type": "string",
          "description": "Who initiated the cancellation."
        },
        "fee": {
          "type": "integer",
          "description": "Cancellation fee in minor currency units."
        },
        "refund_amount": {
          "type": "integer",
          "description": "Amount refunded in minor currency units."
        },
        "canceled_at": {
          "type": "string",
          "format": "date-time",
          "description": "RFC 3339 timestamp."
        }
      }
    },
    "PostPaymentReturnRequest": {
      "type": "object",
      "title": "PostPaymentReturnRequest",
      "description": "Applicable when checkout_systems: redirect is in use. Instructs the business where to redirect the buyer's browser after payment completes, is cancelled, or is abandoned. The platform SHOULD always include this field on the redirect path \u2014 without it, the platform cannot predict or control where the buyer lands after the payment flow. The business MUST issue an HTTP GET redirect to url (with all params appended as URL query parameters) in both terminal outcomes: payment completed and payment cancelled/abandoned.",
      "required": [
        "url"
      ],
      "properties": {
        "url": {
          "type": "string",
          "format": "uri",
          "description": "The platform's return URL. The business MUST redirect the buyer's browser to this URL (via HTTP GET) after payment completes, is cancelled, or is abandoned, appending any params as query parameters."
        },
        "params": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "description": "Query parameter key-value pairs the business MUST append verbatim to url when issuing the return redirect. Keys and values are opaque to the business \u2014 the platform uses them to carry whatever correlation state it needs (e.g. a token generated before the booking request) so it can map the returning buyer back to its session without relying on the booking_id (which is not yet known at request time)."
        }
      }
    }
  },
  "$ref": "#/$defs/Booking"
}