{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://usp.dev/schemas/services/paid_bookings.json",
  "title": "USP Paid Bookings Extension",
  "description": "Extends dev.ucp.shopping.checkout with booking context. UCP assimilation artifact for integrating USP scheduling with UCP checkout. When complete_checkout succeeds, payment and booking are atomically finalized.",
  "allOf": [
    { "$ref": "https://ucp.dev/schemas/shopping/checkout.json" },
    { "$ref": "#/$defs/dev.ucp.shopping.checkout" }
  ],
  "$defs": {
    "dev.ucp.shopping.checkout": {
      "type": "object",
      "description": "Extension properties added to UCP checkout schema for USP paid bookings. Composed into the root via allOf: without that composition this object is declared but applied to nothing, so an instance missing or misshaping booking would validate silently.",
      "properties": {
        "booking": {
          "$ref": "#/$defs/BookingContext",
          "description": "USP booking context embedded in the checkout. Created when checkout is created; confirmed when complete_checkout succeeds."
        },
        "booking_scoped_credential": {
          "$ref": "https://usp.dev/schemas/profile.json#/$defs/BookingScopedCredential",
          "description": "CONDITIONAL, response-only. Sender-constrained capability credential authorizing continuation operations on the booking this checkout created, returned when the business accepts the booking_scoped_credential mechanism. This is where the credential rides in UCP-Native Mode. It is a SIBLING of booking rather than a member of it: BookingContext is scheduling data that platforms persist and re-display, and a credential must never flow there. It is also declared here, inside USP's own registered dev.usp.services.paid_bookings extension, rather than being added as an invented member of the UCP checkout root - the same composition by which this extension already contributes booking. A platform MUST NOT send this on a request, and a business MUST ignore it if received. See specification.md Section 10.1.6."
        }
      }
    },
    "BookingContext": {
      "type": "object",
      "title": "BookingContext",
      "description": "Booking context for UCP checkout. Contains scheduling details that are validated at create_checkout and finalized at complete_checkout.",
      "required": ["service_id", "service_type", "slot"],
      "properties": {
        "booking_id": {
          "type": "string",
          "description": "Unique booking identifier. Generated by the business when the checkout is created (response only)."
        },
        "service_id": {
          "type": "string",
          "description": "The service being booked."
        },
        "service_type": {
          "type": "string",
          "description": "The service vertical. One of the core verticals (appointment, group, reservation, rental) or a vendor-defined vertical using reverse-domain notation."
        },
        "slot": {
          "$ref": "#/$defs/SlotReference",
          "description": "The booked time slot."
        },
        "hold_id": {
          "type": "string",
          "description": "The hold ID if a slot was held. Present only when the business supports holds (holds: true on the availability capability)."
        },
        "resources": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "id": { "type": "string" },
              "type": { "type": "string" },
              "name": { "type": "string" }
            }
          },
          "description": "Requested resources (e.g., specific staff, room)."
        },
        "party_size": {
          "type": "integer",
          "default": 1,
          "description": "Number of participants."
        },
        "confirmation_mode": {
          "type": "string",
          "enum": ["auto", "manual"],
          "description": "auto or manual."
        },
        "booking_status": {
          "type": "string",
          "enum": ["pending", "confirmed", "canceled"],
          "description": "Checkout-scoped status: pending, confirmed, or canceled. Derived from UCP checkout status (see USP specification Section 7.5). Distinct from full Booking.status in booking.json. Response only."
        },
        "actions": {
          "type": "array",
          "items": { "$ref": "#/$defs/BookingContextAction" },
          "description": "Non-payment actions the buyer must complete before payment (e.g., waiver). Payment actions are not used; payment is via UCP checkout."
        },
        "notes": {
          "type": "string",
          "description": "Buyer-provided special requests."
        },
        "recipient": {
          "type": "object",
          "description": "The person receiving the service, when different from the buyer. Same schema as Buyer. When absent, the buyer is the recipient.",
          "properties": {
            "first_name": { "type": "string" },
            "last_name": { "type": "string" },
            "email": { "type": "string", "format": "email" },
            "phone_number": { "type": "string" }
          }
        }
      }
    },
    "BookingContextAction": {
      "type": "object",
      "title": "BookingContextAction",
      "description": "Non-payment action for UCP checkout booking context. Same shape as booking.json Action without payment_context.",
      "required": ["type", "status", "continue_url", "expires_at"],
      "properties": {
        "type": {
          "type": "string",
          "description": "Action type identifier (e.g., waiver, intake_form)."
        },
        "status": {
          "type": "string",
          "enum": ["pending", "completed", "expired"]
        },
        "continue_url": {
          "type": "string",
          "format": "uri",
          "description": "URL to complete this action."
        },
        "expires_at": {
          "type": "string",
          "format": "date-time"
        },
        "message": {
          "$ref": "booking.json#/$defs/Message"
        }
      }
    },
    "SlotReference": {
      "type": "object",
      "title": "SlotReference",
      "description": "Reference to a booked time slot.",
      "required": ["id", "start", "end", "duration"],
      "properties": {
        "id": { "type": "string" },
        "start": { "type": "string", "format": "date-time" },
        "end": { "type": "string", "format": "date-time" },
        "duration": { "type": "string", "description": "ISO 8601 duration" }
      }
    }
  }
}
